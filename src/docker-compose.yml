name: pictureFrame

services:
  web:
    build: .
    ports:
      - "5000:5000"
    restart: unless-stopped
    privileged: true
    devices:
      - /dev/gpiomem:/dev/gpiomem
      - /dev/spidev0.0:/dev/spidev0.0
      - /dev/spidev0.1:/dev/spidev0.1
    volumes:
      - /sys:/sys
      - /home/pi/python/pictureFrame/resources/picture:/data
    environment:
      - FLASK_ENV=production
      - GPIOZERO_PIN_FACTORY=lgpio